# 📝 AI Collaboration Retrospective

## 1. 你用了哪些 AI / Agent？
- Gemini ：用於討論專案架構（SDD）、產出 HTML/CSS/JS 遊戲邏輯與全螢幕動畫特效。
- **VSCode Live Server**：用於本地即時預覽與除錯。

---

## 2. AI 做得最好的一件事是什麼？
**極速建構模組化架構與炫目動畫**。
AI 能在短時間內將遊戲邏輯（`game.js`）與 DOM 控制（`main.js`）妥善解耦（方便進行單元測試），並且快速寫出極具節慶感與衝擊力的 CSS 全螢幕滿版動畫（如滿版震動夜空、玉兔奔月與嫦娥賜福特效）。

---

## 3. AI 搞砸的一件事是什麼？ (AI Failure Incident)

### 發生情境 (Incident)
* **我想要什麼**：在遊戲中加入全螢幕震撼大招（100 次玉兔奔月 / 300 次嫦娥賜福），同時**必須保留原本微觀的點擊反饋（點擊時浮動 `+1` 數字、月餅按鈕點擊縮放、等級進化時的閃爍旋轉動畫）**。
* **AI 做了什麼**：AI 在重構大招特效時發生了「需求遺失與覆蓋」的問題：
  1. 產出的新程式碼漏掉了點擊時顯示 `+1` 浮動文字的邏輯。
  2. 刪除了月餅進化時按鈕的 `.evolve-anim` 旋轉閃爍動畫。
  3. 全螢幕 Overlay 沒有設定好適當的 `z-index` 與 `pointer-events`，一度導致滿版動畫出現時擋住了底下的月餅按鈕，無法繼續連擊。

---

## 4. 你怎麼發現它錯了？
1. **人工手動驗證 (Manual Review & QA)**：在 VSCode 使用 Live Server 開啟網頁點擊測試時，發現點擊月餅後原本活潑的 `+1` 數字消失了，且月餅升級時少了原本的閃爍轉圈動畫。
2. **互動體驗異常**：連續點擊到 100 次時，發現畫面的滿版特效擋住了點擊區域，無法流暢繼續連擊。

---

## 5. 最後怎麼解決？
1. **調整 Prompt 策略**：明確告知 AI 遺漏了什麼（「不要把我原本點一次 +1 刪掉，加回去」、「把原本進化的特效加回去」），並約束 AI 將 UI 微觀反饋與全螢幕宏觀大招獨立開來。
2. **修改 CSS 屬性**：在全螢幕 Overlay 加入 `pointer-events: none` 屬性，確保滿版動畫播放時不影響玩家繼續連擊。
3. **重構點擊事件監聽**：讓點擊事件依序執行：`噴發 +1` ➔ `檢查進化動畫` ➔ `檢查百次全螢幕大招` ➔ `更新 UI`，確保各層級特效互不干擾。

---

## 6. 如果重做一次，你會怎麼改變自己的 AI 協作方式？
* **更嚴謹地維護 SPEC.md**：在請 AI 新增功能或重構前，先將「核心不能被刪除的功能（如 `+1` 浮動動畫、進化效果）」列在 Acceptance Criteria 的 **Must-Have** 列表中，避免 AI 覆蓋舊程式碼。
* **分階段（Incremental）要求，而非一次大改**：每次只讓 AI 專注於單一模組的重構（例如：先單獨擴充 CSS 動畫，確認無誤後再修改 JS 事件邏輯），以降低程式碼被誤刪或功能遺失的風險。